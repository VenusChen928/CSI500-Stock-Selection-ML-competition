# Common Layout

This folder stores shared code that is not a final root entry point but is
still imported by active Stage2 scripts.

## Contents

- `legacy_scripts/`: earlier baseline and utility scripts used by compatibility
  wrappers such as `stage2_tree_consensus.py`.

Generic historical tools that are no longer needed by the active route were
moved to `history/common/tools/`.
