# Legacy Scripts

These scripts are kept because `stage2_tree_consensus.py` imports them for the
active LightGBM/XGBoost consensus sub-route.  They are not final submission
entry points.

Use root-level scripts for final reproduction and validation.
